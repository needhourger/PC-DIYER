import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
  },
  components: {
    Layout: {
      headerBg: "#4a225d",
    },
  },
};

export default theme;
